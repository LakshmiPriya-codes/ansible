- name: demo on vars 
  hosts: local
  connection: local
  vars_prompt:
    - name: username
      prompt: "Please enter username"
      private: false

    - name: password
      prompt: "Please enter password"
      private: True

  tasks: "print information"
  debug:
   msg: "Username is: {{ username }}, password is: {{ password }}"  


