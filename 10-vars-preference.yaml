- name: variables preference
  hosts: frontend
  vars:
    full_name: "Priya from play"
  vars_files:
  - course.yaml
  vars_prompt:
  - name: full_name
    prompt: "Please enter your full_name"
    private: false
  tasks:
  - name: print vars preference
    vars:
      full_name: "Priya from TASK level"
    debug:
      msg: "Hi, {{ full_name }}"